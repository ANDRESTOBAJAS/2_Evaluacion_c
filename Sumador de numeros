#include <stdio.h>

int main() {
    int i, num, sum = 0;

    printf("Ingresa 10 números:\n");

    for (i = 0; i < 5; ++i) {
        printf("Número %d: ", i + 1);
        scanf("%d", &num);
        sum += num;
    }

    printf("La suma de los 10 números es: %d\n", sum);

    return 0;
}
