#include <stdio.h>

int main(void) {
  int x;
  int *px;
  px=&x;//recoge la direccion de x
  x=2;
  printf("\ndireccion de y: %x", &x);
  printf("\nEl valor de x es : %d", x);
  printf("\npuntero a x, px: %x", &px);
  printf("\n El valor de x atraves de su puntero: %d", *px);
}1
